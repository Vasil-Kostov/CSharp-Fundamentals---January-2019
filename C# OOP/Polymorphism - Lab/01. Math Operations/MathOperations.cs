namespace Operations
{
    public class MathOperations
    {
        public int Add(int firstNum, int secondNum)
        {
            return firstNum + secondNum;
        }

        public double Add(double firstNim, double secondNum, double thirdNum)
        {
            return firstNim + secondNum + thirdNum;
        }

        public decimal Add(decimal firstNim, decimal secondNum, decimal thirdNum)
        {
            return firstNim + secondNum + thirdNum;
        }
    }
}
